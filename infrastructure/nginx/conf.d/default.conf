# Conf supplémentaire incluse par nginx.conf (sites / upstreams).
# Ce fichier est volontairement minimal : la configuration principale se
# trouve dans infrastructure/nginx/nginx.conf. Il sert de point d'extension
# pour ajouter des domaines (api.ecoloop.ci, app.ecoloop.ci, admin.ecoloop.ci).

# Exemple d'upstream avec plusieurs instances backend (scale-out) :
#
# upstream backend_pool {
#     least_conn;
#     server backend:8000 max_fails=3 fail_timeout=15s;
#     # server backend2:8000 max_fails=3 fail_timeout=15s;
# }
