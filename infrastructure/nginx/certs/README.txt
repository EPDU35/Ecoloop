# Placeholder pour les certificats TLS.
# En production, déposer ici :
#   fullchain.pem  (certificat + chain)
#   privkey.pem    (clé privée)
# générés via certbot (Let's Encrypt). nginx.conf référence ces chemins.
# Ce dossier est monté en lecture seule dans le conteneur nginx.
